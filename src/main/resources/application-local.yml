spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
    hibernate:
      ddl-auto: validate
  #        use_sql_comments: true

logging:
  level:
    org.hibernate.type.descriptor.sql: trace
#    org.hibernate.SQL: debug